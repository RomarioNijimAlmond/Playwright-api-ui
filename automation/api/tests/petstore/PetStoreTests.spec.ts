import { expect, test } from "@playwright/test";
import { HttpRequests } from "../../infra/rest/HttpRequests";
import { EndPoints } from "../../../common/navigationEnum/EndPoints";

test.describe('api sanity tests for the pet store api', async() => {

    test('', async() => {
        
    })
})
